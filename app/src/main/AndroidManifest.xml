<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.savvari" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="WhatsApp Status Saver"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".Sliders" >
        </activity>
        <activity android:name=".NoStatus" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewStatus" />
        <activity android:name=".Videos" />
        <activity android:name=".Saved" />
        <activity android:name=".Setting" />
        <activity android:name=".Recent" />
        <activity android:name=".IntroOne" />
        <activity android:name=".Splashscreen" >
        </activity>
    </application>

</manifest>